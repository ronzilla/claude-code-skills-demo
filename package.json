{
  "name": "gstats",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Print per-column statistics for a CSV export.",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint src",
    "start": "node src/cli.js"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "prettier": "^3.3.3"
  }
}
