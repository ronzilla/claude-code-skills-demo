{
  "name": "gstats",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Print per-column statistics for a CSV export.",
  "scripts": {
    "format": "oxfmt .",
    "lint": "oxlint src",
    "start": "node src/cli.js"
  },
  "devDependencies": {
    "oxfmt": "^0.4.0",
    "oxlint": "^1.14.0"
  }
}
